import 'package:flutter/material.dart';

class SeanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Détail de la séance')),
      body: Center(child: Text('Enregistrement d'une séance')),
    );
  }
}