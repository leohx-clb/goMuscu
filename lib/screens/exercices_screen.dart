import 'package:flutter/material.dart';

class ExercicesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Liste des exercices')),
      body: Center(child: Text('Afficher tous les exercices')),
    );
  }
}